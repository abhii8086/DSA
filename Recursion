//find the n natural no sum using reacursion.
class Sum {
    public static void main(String[] args) {
        
        System.out.println("Natural No Sum is:"+NaturalSum(5));
    }
    
    public static int NaturalSum(int n){
        if(n==1)
            return 1;
        return n+NaturalSum(n-1);
    }
}

/* Recursion flow:
1.find base case
2.find rel btween prob and sub-problem
3.generalise it into functional format
*/
// finding pow of two no a,b
class Pow {
    public static void main(String[] args) {
        
        System.out.println("Pow No Sum is:"+Pow(2,3));
    }
    
    public static int Pow(int a ,int b){
        if(b==0)
            return 1;
        return a*Pow(a,b-1);
    }
}
